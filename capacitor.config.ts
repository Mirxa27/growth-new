import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.growthechonexus',
  appName: 'growth-echo-nexus',
  webDir: 'dist'
};

export default config;
